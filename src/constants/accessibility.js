// Accessibility option sets. UI scale multiplies the root font size (text-only
// scaling — see the --ui-scale token in style.css and the px→rem font sizes).
export const UI_SCALE_STEPS = [
  { value: 0.9,  label: '90%'  },
  { value: 1,    label: '100%' },
  { value: 1.1,  label: '110%' },
  { value: 1.25, label: '125%' },
];

export const DEFAULT_UI_SCALE = 1;
